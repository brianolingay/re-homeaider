{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build:server": "lerna run build --scope={@homeaider/common,@homeaider/server}",
    "build:web": "lerna run build --scope={@homeaider/common,@homeaider/web}"
  },
  "resolutions": {
    "graphql": "14.3.1",
    "@types/react": "16.8.20",
    "@types/react-dom": "16.8.4"
  },
  "devDependencies": {
    "lerna": "^3.10.7"
  }
}
