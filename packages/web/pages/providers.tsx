import * as React from "react";
import { Container, Message } from "semantic-ui-react";

export const Providers = () => (
  <Container>
    <Message
      header="Providers for approval"
      content="This page is still on work, so please be patient!"
    />
  </Container>
);
